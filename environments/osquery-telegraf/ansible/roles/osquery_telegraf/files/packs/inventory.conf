{
  "queries": {
    "users": {
      "query": "SELECT uid, gid, username, description, directory FROM users;",
      "interval": 14400
    },
    "installed_packages": {
      "query": "SELECT name, version, install_time FROM packages;",
      "interval": 86400,
      "snapshot": true
    }
  }
}
