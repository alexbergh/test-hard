# Default credentials for the Wazuh stack used by the local docker-compose harness.
# Replace these values with strong, unique secrets before running the services.
WAZUH_INDEXER_PASSWORD=SecretPassword123!
WAZUH_API_PASSWORD=AnotherSecretPassword456!
