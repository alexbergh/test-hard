Рабочий конфиг 

services:
  wazuh-indexer:
    build:
      context: .
      dockerfile: Dockerfile.wazuh-indexer
    container_name: wazuh-indexer
    restart: unless-stopped
    ports:
      - "9200:9200"
    environment:
      - OPENSEARCH_JAVA_OPTS=-Xms1g -Xmx1g
    volumes:
      - wazuh-indexer-data:/var/lib/wazuh-indexer
      - ${PWD}/tests/docker/config/wazuh-indexer/opensearch.yml:/usr/share/wazuh-indexer/opensearch.yml:ro
      - ${PWD}/config/wazuh-indexer/certs:/usr/share/wazuh-indexer/certs:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -fsSk -u admin:${WAZUH_INDEXER_PASSWORD} https://localhost:9200/_cluster/health?pretty || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5

volumes:
  wazuh-indexer-data:
  wazuh-indexer-logs:
